import { createTheme } from '@material-ui/core';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#4338CA',
      contrastText: '#ffffff',
    },
  },
  typography: {
    fontFamily: 'Inter UI',
  },
});
